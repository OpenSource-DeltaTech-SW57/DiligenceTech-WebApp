import { ProjectMemberEntity } from './project-member.entity';

describe('ProjectMemberEntity', () => {
  it('should create an instance', () => {
    expect(new ProjectMemberEntity()).toBeTruthy();
  });
});
