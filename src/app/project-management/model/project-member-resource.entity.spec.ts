import { ProjectMemberResourceEntity } from './project-member-resource.entity';

describe('ProjectMemberResourceEntity', () => {
  it('should create an instance', () => {
    expect(new ProjectMemberResourceEntity()).toBeTruthy();
  });
});
